group :development, :testing do
  gem 'dotenv'
  gem 'pry'
end

gem 'google_drive'

gem 'rake'
gem 'warning'

# TODO: Change to published version before first releasee. ;)
gem 'issola', path: '~/dev/discord/issola/'
